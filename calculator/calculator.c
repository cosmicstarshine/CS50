#include <stdio.h>

int main(){
    long long x;
    long long y;
    printf("Enter an integer for x: ");
    scanf("%lli", &x);
    printf("Enter an integer for y: ");
    scanf("%lli", &y);
    printf("The sum of x and y is: %lli\n", x + y);
    return 0;    

}